# CANON_GOVERNING_LAYER

The governing layer is:

x_{t+1} = Pi_K(F(x_t))

All system behavior must reduce to:
1. latent evolution (F)
2. constraint projection (Pi_K)

Operators are a decomposition of F and Pi_K.
They do not replace the governing equation.

Observation is subordinate.